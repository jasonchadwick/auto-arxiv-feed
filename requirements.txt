# Core runtime dependencies
feedparser>=6.0
requests>=2.31
numpy>=1.26
scikit-learn>=1.4
pyyaml>=6.0
pyzotero>=1.5

# Embedding API providers (install only what you need)
openai>=1.30                  # for provider: openai
google-genai>=0.8              # for provider: gemini
# anthropic                   # no embedding API available yet

# Local embedding model (for provider: local)
# sentence-transformers>=3.0  # required
#
# CPU only:
#   pip install sentence-transformers
#
# CUDA GPU (Linux / WSL2 with NVIDIA driver — no extra CUDA toolkit needed):
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
#   pip install sentence-transformers
#
# Recommended models (downloaded automatically from HuggingFace on first use):
#   BAAI/bge-small-en-v1.5   384-dim, ~133 MB  — default, fast
#   BAAI/bge-large-en-v1.5   1024-dim, ~1.3 GB — highest quality
#   allenai-specter          768-dim, ~400 MB  — tuned for scientific papers
#   all-MiniLM-L6-v2         384-dim,  ~22 MB  — smallest/fastest

# Development / testing
pytest>=8.0
